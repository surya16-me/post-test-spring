server:
  port: 9090

spring:
  application:
    name: crud-service
  datasource:
    url: jdbc:postgresql://localhost:5432/post-test?currentSchema=public
    username: postgres
    password: Sunzblade123&
    driver-class-name: org.postgresql.Driver
    maximumPoolSize: 10
  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: update
    show-sql: true
    generate-ddl: true